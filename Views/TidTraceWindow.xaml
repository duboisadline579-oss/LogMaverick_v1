<Window x:Class="LogMaverick.Views.TidTraceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TID Explorer" Height="500" Width="800" WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
        Background="#161616" Foreground="White">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock x:Name="TidTitle" Foreground="#007AFF" FontSize="14" FontWeight="Bold" VerticalAlignment="Center"/>
            <TextBlock Text=" — 실시간 업데이트 중" Foreground="#555" FontSize="11" VerticalAlignment="Center"/>
        </StackPanel>
        <Grid Grid.Row="1" Margin="0,0,0,8">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <TextBox x:Name="TxtSearch" Background="#0D0D0D" Foreground="White" BorderBrush="#444" BorderThickness="1" Height="30" VerticalContentAlignment="Center" Padding="8,0" FontSize="12" TextChanged="Search_Changed" ToolTip="TID 로그 내 키워드 검색"/>
            <TextBlock Grid.Column="0" Text="🔍 검색..." Foreground="#555" FontSize="12" VerticalAlignment="Center" Margin="10,0" IsHitTestVisible="False" x:Name="SearchHint"/>
            <TextBlock Grid.Column="1" x:Name="TxtCount" Foreground="#888" FontSize="11" VerticalAlignment="Center" Margin="8,0"/>
        </Grid>
        <ListView x:Name="LogList" Grid.Row="2" MouseDoubleClick="Log_DoubleClick" Background="#0F0F0F" Foreground="#DDD" BorderThickness="0">
            <ListView.View><GridView>
                <GridViewColumn Header="Time" DisplayMemberBinding="{Binding Time, StringFormat={}{0:HH:mm:ss}}" Width="90"/>
                <GridViewColumn Header="Category" DisplayMemberBinding="{Binding Category}" Width="90"/>
                <GridViewColumn Header="Type" DisplayMemberBinding="{Binding Type}" Width="80"/>
                <GridViewColumn Header="Message" DisplayMemberBinding="{Binding Message}" Width="470"/>
            </GridView></ListView.View>
        </ListView>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="COPY ALL" Click="Copy_Click" Width="80" Height="30" Background="#2A2A2A" Foreground="#CCC" BorderBrush="#444" Margin="0,0,8,0" ToolTip="전체 로그 클립보드 복사"/>
            <Button Content="CLOSE" Click="Close_Click" Width="80" Height="30" Background="#007AFF" Foreground="White" BorderThickness="0"/>
        </StackPanel>
    </Grid>
</Window>
