<Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                    <Grid Margin="0,6,0,6">
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                        <TextBox x:Name="TxtSearch" Background="#0D0D0D" Foreground="White" BorderBrush="#444" BorderThickness="1" Height="28" VerticalContentAlignment="Center" Padding="8,0" FontSize="11" TextChanged="Search_Changed"/>
                        <TextBlock Grid.Column="0" Text="🔍 Key/Value 검색..." Foreground="#555" FontSize="11" VerticalAlignment="Center" Margin="10,0" IsHitTestVisible="False" x:Name="SearchHint"/>
                        <TextBlock Grid.Column="1" x:Name="TxtMatchCount" Foreground="#888" FontSize="11" VerticalAlignment="Center" Margin="8,0"/>
                    </Grid>
                    <ListView x:Name="KvList" Grid.Row="1" Background="#0F0F0F" Foreground="#DDDDDD" BorderThickness="0" SelectionMode="Extended">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Key" Width="220">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Key}" Foreground="{Binding KeyColor}" FontWeight="Bold" FontFamily="Consolas" FontSize="12"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Value" Width="540">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBox Text="{Binding Value, Mode=OneWay}" Foreground="{Binding ValueColor}" FontFamily="Consolas" FontSize="11" TextWrapping="Wrap" IsReadOnly="True" Background="Transparent" BorderThickness="0" MaxHeight="150" VerticalScrollBarVisibility="Auto" PreviewMouseDown="ValueBox_PreviewMouseDown"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </Grid>
            </TabItem>
            <TabItem Header="📄 원문 (Pretty)">
                <TextBox x:Name="TxtPretty" Background="#0A0A0A" Foreground="#AADDFF" BorderThickness="0" FontFamily="Consolas" FontSize="12" IsReadOnly="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Padding="10" AcceptsReturn="True"/>
            </TabItem>
            <TabItem Header="📝 Raw">
                <TextBox x:Name="TxtRaw" Background="#0A0A0A" Foreground="#DDDDDD" BorderThickness="0" FontFamily="Consolas" FontSize="12" IsReadOnly="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Padding="10" AcceptsReturn="True"/>
            </TabItem>
        </TabControl>
        <TextBlock Grid.Row="2" Text="💡 Ctrl+C: 선택 Value 복사  |  파싱뷰에서 Value 클릭 후 드래그하여 텍스트 선택 가능" Foreground="#555" FontSize="10" Margin="0,6,0,0"/>
    </Grid>
</Window>
