<Window x:Class="LogMaverick.Views.ErrorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="EXCEPTIONS" Height="650" Width="1000"
        Background="#0D0D0D" WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <Style x:Key="TabStyle" TargetType="TabItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="Bd" Background="#1A1A1A" Padding="14,8" Margin="0,0,2,0">
                            <ContentPresenter x:Name="Cp" ContentSource="Header" TextBlock.Foreground="#888" TextBlock.FontWeight="Bold" TextBlock.FontSize="12"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#007AFF"/>
                                <Setter TargetName="Cp" Property="TextBlock.Foreground" Value="#FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#0055CC"/>
                                <Setter TargetName="Cp" Property="TextBlock.Foreground" Value="#FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ListView">
            <Setter Property="Background" Value="#0A0A0A"/>
            <Setter Property="Foreground" Value="#DDDDDD"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="VirtualizingStackPanel.IsVirtualizing" Value="True"/>
            <Setter Property="VirtualizingStackPanel.VirtualizationMode" Value="Recycling"/>
        </Style>
        <Style TargetType="GridViewColumnHeader">
            <Setter Property="Background" Value="#1A1A1A"/>
            <Setter Property="Foreground" Value="#AAAAAA"/>
            <Setter Property="BorderBrush" Value="#333"/>
        </Style>
    </Window.Resources>
    <Grid Margin="12">
        <TabControl Background="Transparent" BorderThickness="0">
            <TabControl.Resources>
                <Style TargetType="TabItem" BasedOn="{StaticResource TabStyle}"/>
            </TabControl.Resources>
            <TabItem Header="⚠ 에러 히스토리">
                <Grid>
                    <Grid.RowDefinitions><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                    <ListView x:Name="ErrorList" MouseDoubleClick="Error_DoubleClick">
                        <ListView.View><GridView>
                            <GridViewColumn Header="Time" DisplayMemberBinding="{Binding Time, StringFormat={}{0:HH:mm:ss}}" Width="90"/>
                            <GridViewColumn Header="Type" DisplayMemberBinding="{Binding Type}" Width="90"/>
                            <GridViewColumn Header="TID" DisplayMemberBinding="{Binding Tid}" Width="70"/>
                            <GridViewColumn Header="Category" DisplayMemberBinding="{Binding Category}" Width="80"/>
                            <GridViewColumn Header="Message" DisplayMemberBinding="{Binding Message}" Width="560"/>
                        </GridView></ListView.View>
                    </ListView>
                    <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
                        <Button Content="CLEAR" Click="Clear_Click" Width="80" Height="28" Background="#2A2A2A" Foreground="#CCC" BorderBrush="#444" Margin="0,0,8,0"/>
                        <Button Content="CLOSE" Click="Close_Click" Width="80" Height="28" Background="#007AFF" Foreground="White" BorderThickness="0"/>
                    </StackPanel>
                </Grid>
            </TabItem>
            <TabItem Header="🎨 색상 키워드">
                <Grid Margin="8">
                    <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                    <TextBlock Foreground="#888" FontSize="11" Margin="0,0,0,8" TextWrapping="Wrap">
                        <Run Text="키워드 포함 로그에 색상 강조 및 알림 적용. 예: ERROR=#FF4500, TIMEOUT=#FFD700"/>
                    </TextBlock>
                    <ListView x:Name="KeywordList" Grid.Row="1" MouseDoubleClick="Keyword_DoubleClick">
                        <ListView.View><GridView>
                            <GridViewColumn Header="키워드" Width="200">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Keyword}" Foreground="{Binding Color}" FontWeight="Bold" FontSize="12"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="색상" Width="100">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border Background="{Binding Color}" Width="60" Height="18" CornerRadius="3"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="알림" DisplayMemberBinding="{Binding Notify}" Width="60"/>
                        </GridView></ListView.View>
                    </ListView>
                    <Grid Grid.Row="2" Margin="0,8,0,0">
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="120"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                        <TextBox x:Name="TxtKeyword" Background="#1A1A1A" Foreground="White" BorderBrush="#444" BorderThickness="1" Padding="8,5" ToolTip="키워드 입력 (예: TIMEOUT)"/>
                        <TextBox x:Name="TxtColor" Grid.Column="1" Background="#1A1A1A" Foreground="White" BorderBrush="#444" BorderThickness="1" Padding="8,5" Text="#FF4500" Margin="4,0" ToolTip="색상 코드 (예: #FF4500)"/>
                        <CheckBox x:Name="ChkNotify" Grid.Column="2" Content="알림" Foreground="#CCC" VerticalAlignment="Center" IsChecked="True" Margin="4,0"/>
                        <Button Grid.Column="3" Content="+ ADD" Click="AddKeyword_Click" Width="70" Height="30" Background="#007AFF" Foreground="White" BorderThickness="0" Margin="4,0"/>
                        <Button Grid.Column="4" Content="DELETE" Click="DelKeyword_Click" Width="70" Height="30" Background="#1A1A1A" Foreground="#FF4500" BorderBrush="#FF4500" BorderThickness="1"/>
                    </Grid>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
